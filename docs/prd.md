---
AIGC:
    ContentProducer: Minimax Agent AI
    ContentPropagator: Minimax Agent AI
    Label: AIGC
    ProduceID: "00000000000000000000000000000000"
    PropagateID: "00000000000000000000000000000000"
    ReservedCode1: 3046022100dc3ea42d262b3296b6aa6f53986ad2d95467ce22e8273034db860d4bbbd8e5f1022100f18651c0d3deb6bfdb2dd429a43dd8cf69b9e718c5c144a6233baa6f769352d5
    ReservedCode2: 3045022100eddbc95e8727fc5ac2506dd75279051a1dbf2c9226987b55c1ce02b237d9003f022074d714ca8a4247dd0528b2698c33a09c3f7a8719d1a9ed7f5177f95cba6ace53
---

# 《经典炸弹人》复刻版 - 简化Demo PRD

## 1. 项目目标

聚焦于实现**单一可玩关卡**、**单人模式**和**键盘控制**，以最快速度构建一个可运行、可体验的核心玩法原型,验证操作手感和基础循环。

## 2. 核心功能范围(最小可行产品)

### 2.1 玩家控制

- **操作设备**:仅支持键盘。

- **默认键位**:

    - **移动**:`W`(上),`A`(左),`S`(下),`D`(右)

    - **放置炸弹**:`空格键`

    - **重新开始**:`R`键(死亡或通关后)

    - **退出游戏**:`Esc`键

### 2.2 游戏模式

- **仅单人闯关模式**:目标是在一张精心设计的地图中,消灭所有敌人,找到隐藏的出口。

### 2.3 游戏实体(精简列表)

| 实体 | 数量/类型 | 说明 |

| :--- | :--- | :--- |

| **玩家** | 1个 | 唯一可控角色。 |

| **软墙** | 约30-40个 | 可被炸弹摧毁,摧毁后有概率掉落道具。 |

| **硬墙** | 固定布局 | 构成迷宫不可摧毁的边界。 |

| **敌人** | 2种类型,共4-6个 | 1. **基础型**:沿直线运动,碰墙随机转向。<br>2. **追踪型**:感知到玩家在一定范围内时,会尝试靠近。 |

| **炸弹** | 玩家同时最多放置1个(默认) | 放置后2秒爆炸,十字火焰,遇墙停止。 |

| **道具** | 3种核心类型 | 仅火焰增强、炸弹数量、速度提升。 |

| **出口** | 1个 | 初始隐藏在软墙下,消灭所有敌人后显现。 |

## 3. 单张地图设计方案

地图采用固定设计(13x11格),确保有探索和策略空间。

### 3.1 布局示意图

```
█  █████████████
█  P ▒ ▒ █ ▒ ▒ █
█  █▒█▒█▒█▒█▒█ █
█  ▒ ▒ ▒ ▒ ▒ ▒ █
█▒█▒█ ███ █▒█▒█
█E ▒ █   █ ▒ ▒X█
█▒█▒█ ███ █▒█▒█
█  ▒ ▒ ▒ ▒ ▒ ▒ █
█  █▒█▒█▒█▒█▒█ █
█  ▒ █ ▒ ▒ █ ▒ █
████████████████
```

**图例说明**:

- `█`: **硬墙**(不可摧毁)

- `▒`: **软墙**(可摧毁)

- `P`: **玩家**初始位置(被少量软墙保护)

- `E`: **敌人**初始分布点(共4-6个)

- `X`: **隐藏出口**位置(被一个软墙覆盖)

- `空格`: **空地**

### 3.2 地图设计要点

1.  **保护性开局**:玩家出生点周围有少量软墙,提供短暂安全期。

2.  **对称与通道**:地图基本对称,但通过软墙布局制造多条可开辟的路径,鼓励探索。

3.  **中心开阔区**:地图中心区域较为开阔,是可能发生多敌遭遇战的区域。

4.  **出口隔离**:出口被安排在角落,并用软墙包围,增加最终目标感。

## 4. 简化游戏流程

1.  **开始**:玩家出现在 `P` 点。

2.  **核心循环**:

    - 移动 -> 放置炸弹炸开软墙 -> 获取道具 -> 消灭遭遇的敌人。

    - 策略选择:是先开路,还是先诱杀敌人。

3.  **胜利条件**:

    - **消灭所有敌人**后,覆盖出口 `X` 的软墙自动消失(或发出明显光芒)。

    - 玩家移动至 `X` 点,弹出"通关"提示。

4.  **失败条件**:玩家被火焰击中或触碰敌人。显示"失败"提示,并按`R`键重试。

## 5. 简化道具系统

仅实现三种最核心、最能改变游戏体验的道具。所有道具均通过炸毁软墙随机掉落(掉落率建议50%),外观用不同颜色方块区分。

| 道具 | 外观(色块) | 效果 | 备注 |

| :--- | :--- | :--- | :--- |

| **火焰增强** | 🔴 红色方块 | 炸弹火焰向每个方向延伸的**格子数+1** | 基础为1格,最多可叠加至3-4格 |

| **炸弹数量** | 🔵 蓝色方块 | 玩家可同时放置的**炸弹数量+1** | 基础为1个,最多可叠加至3个 |

| **速度提升** | 🟢 绿色方块 | 玩家移动速度提升**20%** | 可叠加,但有上限 |

## 6. 技术实现方案（基于Pygame）

### 6.1 技术栈选型

| 技术组件 | 选型 | 版本要求 | 说明 |
| :--- | :--- | :--- | :--- |
| **核心框架** | Pygame | >= 2.5.0 | 成熟的2D游戏开发框架,跨平台支持良好 |
| **编程语言** | Python | >= 3.10 | 语法简洁,开发效率高 |
| **打包工具** | PyInstaller | >= 6.0.0 | 支持将Python应用打包为独立可执行文件 |
| **音频处理** | Pygame.mixer | 内置 | 处理音效和背景音乐 |
| **资源管理** | 自定义加载器 | - | 统一管理图像、音频、配置文件 |

### 6.2 项目结构设计

```
bomberman/
├── main.py                 # 程序入口
├── requirements.txt        # Python依赖清单
├── build_config.py         # PyInstaller打包配置
├── README.md               # 项目说明文档
│
├── src/                    # 源代码目录
│   ├── __init__.py
│   ├── game.py             # 游戏主循环类
│   ├── config.py           # 全局配置(窗口大小、FPS、键位等)
│   │
│   ├── entities/           # 游戏实体模块
│   │   ├── __init__.py
│   │   ├── player.py       # 玩家类
│   │   ├── enemy.py        # 敌人基类和子类
│   │   ├── bomb.py         # 炸弹类
│   │   ├── explosion.py    # 爆炸效果类
│   │   ├── powerup.py      # 道具类
│   │   └── tile.py         # 地图块(硬墙、软墙、出口)
│   │
│   ├── systems/            # 游戏系统模块
│   │   ├── __init__.py
│   │   ├── grid.py         # 网格系统(坐标转换、碰撞检测)
│   │   ├── collision.py    # 碰撞管理器
│   │   ├── spawner.py      # 道具生成器
│   │   └── ai.py           # 敌人AI逻辑
│   │
│   ├── scenes/             # 场景管理
│   │   ├── __init__.py
│   │   ├── base_scene.py   # 场景基类
│   │   ├── game_scene.py   # 游戏关卡场景
│   │   ├── menu_scene.py   # 菜单场景(可选)
│   │   └── result_scene.py # 结算画面(胜利/失败)
│   │
│   ├── ui/                 # UI组件
│   │   ├── __init__.py
│   │   ├── hud.py          # 游戏内HUD显示
│   │   └── text_render.py  # 文本渲染工具
│   │
│   └── utils/              # 工具模块
│       ├── __init__.py
│       ├── assets.py       # 资源加载管理器
│       ├── constants.py    # 常量定义(实体类型、状态枚举等)
│       └── helpers.py      # 辅助函数(坐标转换等)
│
├── assets/                 # 游戏资源目录
│   ├── images/             # 图像资源
│   │   ├── player.png
│   │   ├── enemy_basic.png
│   │   ├── enemy_chase.png
│   │   ├── wall_hard.png
│   │   ├── wall_soft.png
│   │   ├── bomb.png
│   │   ├── explosion.png
│   │   ├── powerup_fire.png
│   │   ├── powerup_bomb.png
│   │   ├── powerup_speed.png
│   │   └── exit.png
│   │
│   ├── sounds/             # 音效资源
│   │   ├── bomb_place.wav
│   │   ├── bomb_explode.wav
│   │   ├── powerup_get.wav
│   │   ├── enemy_die.wav
│   │   └── game_over.wav
│   │
│   ├── fonts/              # 字体文件
│   │   └── game_font.ttf
│   │
│   └── maps/               # 地图数据
│       └── level_01.json   # 地图配置(网格布局、敌人位置等)
│
├── dist/                   # 打包输出目录(自动生成)
│   ├── bomberman_linux/
│   ├── bomberman_macos/
│   └── bomberman_windows/
│
└── tests/                  # 单元测试(可选)
    ├── __init__.py
    └── test_grid.py
```

### 6.3 核心模块设计

#### 6.3.1 游戏主循环（game.py）

```python
# 伪代码示意
class Game:
    def __init__(self):
        pygame.init()
        self.screen = pygame.display.set_mode((WIDTH, HEIGHT))
        self.clock = pygame.time.Clock()
        self.current_scene = GameScene(self)

    def run(self):
        """主游戏循环"""
        running = True
        while running:
            # 1. 事件处理
            events = pygame.event.get()
            for event in events:
                if event.type == pygame.QUIT:
                    running = False
                self.current_scene.handle_event(event)

            # 2. 更新逻辑(固定时间步长)
            dt = self.clock.tick(FPS) / 1000.0
            self.current_scene.update(dt)

            # 3. 渲染画面
            self.current_scene.render(self.screen)
            pygame.display.flip()

        pygame.quit()
```

#### 6.3.2 网格系统（grid.py）

```python
# 核心功能伪代码
class Grid:
    def __init__(self, cols, rows, tile_size):
        self.cols = cols  # 13
        self.rows = rows  # 11
        self.tile_size = tile_size  # 48像素
        self.tiles = [[None for _ in range(cols)] for _ in range(rows)]

    def world_to_grid(self, x, y):
        """像素坐标转网格坐标"""
        return int(x // self.tile_size), int(y // self.tile_size)

    def grid_to_world(self, col, row):
        """网格坐标转像素坐标(中心点)"""
        return col * self.tile_size + self.tile_size // 2, \
               row * self.tile_size + self.tile_size // 2

    def is_walkable(self, col, row):
        """检查格子是否可通行"""
        if not (0 <= col < self.cols and 0 <= row < self.rows):
            return False
        tile = self.tiles[row][col]
        return tile is None or tile.type in [EMPTY, POWERUP]

    def get_neighbors(self, col, row):
        """获取四方向邻居格子(用于爆炸传播)"""
        directions = [(0, -1), (1, 0), (0, 1), (-1, 0)]
        neighbors = []
        for dx, dy in directions:
            nc, nr = col + dx, row + dy
            if 0 <= nc < self.cols and 0 <= nr < self.rows:
                neighbors.append((nc, nr, self.tiles[nr][nc]))
        return neighbors
```

#### 6.3.3 炸弹与爆炸系统（bomb.py / explosion.py）

```python
# 炸弹类伪代码
class Bomb:
    def __init__(self, col, row, power, owner):
        self.col = col
        self.row = row
        self.power = power  # 火焰长度
        self.owner = owner  # 玩家引用
        self.timer = 2.0    # 2秒倒计时
        self.exploded = False

    def update(self, dt, grid):
        self.timer -= dt
        if self.timer <= 0 and not self.exploded:
            self.explode(grid)
            self.exploded = True

    def explode(self, grid):
        """生成十字火焰"""
        explosions = []
        directions = [(0, -1), (1, 0), (0, 1), (-1, 0)]

        # 中心爆炸
        explosions.append(Explosion(self.col, self.row, 0.3))

        # 四个方向扩散
        for dx, dy in directions:
            for step in range(1, self.power + 1):
                nc, nr = self.col + dx * step, self.row + dy * step
                tile = grid.tiles[nr][nc]

                if tile.type == HARD_WALL:
                    break  # 硬墙阻挡

                explosions.append(Explosion(nc, nr, 0.3))

                if tile.type == SOFT_WALL:
                    tile.destroy()  # 摧毁软墙
                    break  # 停止扩散

        return explosions
```

#### 6.3.4 敌人AI（ai.py）

```python
# 基础型敌人
class BasicEnemyAI:
    def __init__(self, enemy):
        self.enemy = enemy
        self.direction = random.choice(['up', 'down', 'left', 'right'])

    def update(self, grid, dt):
        """尝试沿当前方向移动,碰壁则随机转向"""
        next_col, next_row = self._get_next_position()

        if grid.is_walkable(next_col, next_row):
            self.enemy.move_to(next_col, next_row)
        else:
            self.direction = self._choose_random_direction(grid)

    def _choose_random_direction(self, grid):
        """从可行方向中随机选择"""
        valid_dirs = []
        for direction in ['up', 'down', 'left', 'right']:
            nc, nr = self._get_next_pos_in_dir(direction)
            if grid.is_walkable(nc, nr):
                valid_dirs.append(direction)
        return random.choice(valid_dirs) if valid_dirs else self.direction

# 追踪型敌人
class ChaseEnemyAI:
    def __init__(self, enemy):
        self.enemy = enemy
        self.chase_range = 5  # 感知范围

    def update(self, grid, player, dt):
        """计算曼哈顿距离,在范围内则朝玩家移动"""
        distance = abs(self.enemy.col - player.col) + abs(self.enemy.row - player.row)

        if distance <= self.chase_range:
            # 简化追踪:优先减少X或Y方向距离
            if abs(self.enemy.col - player.col) > abs(self.enemy.row - player.row):
                direction = 'right' if player.col > self.enemy.col else 'left'
            else:
                direction = 'down' if player.row > self.enemy.row else 'up'

            nc, nr = self._get_next_pos_in_dir(direction)
            if grid.is_walkable(nc, nr):
                self.enemy.move_to(nc, nr)
        else:
            # 超出范围则随机游荡
            BasicEnemyAI(self.enemy).update(grid, dt)
```

### 6.4 资源规范

#### 6.4.1 图像资源

- **格式**: PNG（支持透明通道）
- **尺寸**: 统一使用 `48x48` 像素（单格尺寸）
- **命名**: 小写字母+下划线（如 `enemy_basic.png`）
- **简化方案**: 初期可使用纯色方块占位,后期替换为像素艺术风格精灵

#### 6.4.2 音效资源

- **格式**: WAV（兼容性好）或 OGG（体积小）
- **采样率**: 44.1kHz
- **时长**: 音效 < 2秒,保持简短有力
- **音量**: 统一归一化处理,避免音量差异

#### 6.4.3 地图配置（JSON格式）

```json
{
  "width": 13,
  "height": 11,
  "tile_size": 48,
  "tiles": [
    "█  █████████████",
    "█  P ▒ ▒ █ ▒ ▒ █",
    "█  █▒█▒█▒█▒█▒█ █",
    ...
  ],
  "enemies": [
    {"type": "basic", "col": 1, "row": 5},
    {"type": "chase", "col": 11, "row": 5}
  ],
  "exit": {"col": 11, "row": 5}
}
```

### 6.5 跨平台打包方案

#### 6.5.1 PyInstaller配置文件（build_config.py）

```python
# PyInstaller配置脚本
import PyInstaller.__main__
import platform

def build_for_current_platform():
    system = platform.system().lower()

    common_args = [
        'main.py',
        '--name=Bomberman',
        '--windowed',  # 隐藏控制台窗口
        '--onefile',   # 单文件模式(可选,也可用 --onedir)
        '--add-data=assets:assets',  # 打包资源目录
        '--icon=assets/images/icon.ico',  # 应用图标
        '--clean',
    ]

    # 平台特定配置
    if system == 'darwin':  # macOS
        common_args.extend([
            '--osx-bundle-identifier=com.yourgame.bomberman',
            '--target-arch=universal2',  # 支持Intel和Apple Silicon
        ])
    elif system == 'windows':
        common_args.extend([
            '--version-file=version_info.txt',  # Windows版本信息
        ])

    PyInstaller.__main__.run(common_args)

if __name__ == '__main__':
    build_for_current_platform()
```

#### 6.5.2 打包命令与流程

**开发依赖安装**:
```bash
pip install pygame>=2.5.0 pyinstaller>=6.0.0
```

**单平台打包（在目标系统上执行）**:
```bash
# 方式1: 直接使用PyInstaller
pyinstaller --clean --windowed --onefile \
  --add-data "assets:assets" \
  --name Bomberman \
  main.py

# 方式2: 使用配置脚本
python build_config.py
```

**输出目录**:
- **单文件模式** (`--onefile`): `dist/Bomberman` 或 `dist/Bomberman.exe`
- **文件夹模式** (`--onedir`): `dist/Bomberman/` 目录（包含所有依赖）

#### 6.5.3 多平台打包策略

| 目标平台 | 打包环境要求 | 输出物 | 注意事项 |
| :--- | :--- | :--- | :--- |
| **Windows** | Windows 10+ | `Bomberman.exe` | 需在Windows上打包,建议使用虚拟机或CI/CD |
| **macOS** | macOS 10.15+ | `Bomberman.app` | 需在Mac上打包,注意代码签名和公证（分发时） |
| **Linux** | Ubuntu/Debian | `Bomberman` | 建议提供AppImage或打包为.deb |

**自动化方案（可选）**:
- 使用 **GitHub Actions** 或 **GitLab CI** 配置多平台自动打包流程
- 示例工作流: 为每个提交自动生成 Windows/macOS/Linux 三平台构建产物

#### 6.5.4 打包后测试清单

- [ ] 应用能否正常启动
- [ ] 资源文件是否正确加载（图像、音效）
- [ ] 键盘输入是否响应
- [ ] 游戏循环是否流畅（目标60FPS）
- [ ] 退出游戏是否正常（无残留进程）
- [ ] 文件大小是否合理（< 50MB）

### 6.6 开发环境要求

| 组件 | 最低版本 | 推荐版本 | 说明 |
| :--- | :--- | :--- | :--- |
| **Python** | 3.10 | 3.11+ | 支持最新语法特性 |
| **操作系统** | - | - | Windows 10+, macOS 10.15+, Ubuntu 20.04+ |
| **内存** | 4GB | 8GB+ | 开发时运行IDE+游戏 |
| **IDE推荐** | - | VS Code / PyCharm | 支持Python调试和代码补全 |

### 6.7 性能优化要点

1. **帧率控制**: 使用 `clock.tick(60)` 限制在60FPS,保证跨平台一致性
2. **图像缓存**: 使用 `pygame.image.load().convert_alpha()` 预加载并转换图像格式
3. **音效预加载**: 游戏启动时一次性加载所有音效到内存
4. **脏矩形优化**: 仅重绘变化区域（高级优化,初期可不实现）
5. **网格索引**: 使用二维数组快速查询实体所在格子,避免遍历所有实体

## 7. 界面与反馈

- **游戏内HUD**(屏幕左上角):

    ```
    剩余敌人: 4
    炸弹威力: ■■■ (火焰长度图标化)
    炸弹数量: 💣💣 (图标数量表示)
    ```

- **关键反馈**:

    - 放置炸弹时,发出"滴"声。

    - 炸弹爆炸和墙体被毁,有屏幕轻微震动和音效。

    - 获得道具时有清脆音效和角色身上闪光。

    - 敌人被消灭时,有爆炸动画和音效。

- **结束画面**:简单的"**任务完成!**"或"**被击败了...**"文字提示,并说明按 `R` 重试。

---

## 8. 开发路线图

### 8.1 阶段划分

#### **阶段一：基础框架搭建（预计2-3天）**

**目标**: 建立可运行的游戏窗口和基础游戏循环

- [ ] 创建项目结构和目录
- [ ] 实现 `main.py` 和 `game.py` 主循环
- [ ] 实现配置管理 (`config.py`)
- [ ] 实现资源加载器 (`assets.py`)
- [ ] 创建占位图像（纯色方块）
- [ ] 测试：窗口能正常启动并以60FPS运行

**交付物**: 可运行的空白游戏窗口

---

#### **阶段二：网格系统与地图（预计2-3天）**

**目标**: 实现网格系统和静态地图渲染

- [ ] 实现 `Grid` 类（坐标转换、边界检测）
- [ ] 实现 `Tile` 类（硬墙、软墙）
- [ ] 实现地图加载器（从JSON读取地图数据）
- [ ] 渲染静态地图到屏幕
- [ ] 测试：地图正确显示,网格坐标转换准确

**交付物**: 显示完整地图的静态画面

---

#### **阶段三：玩家控制（预计1-2天）**

**目标**: 实现玩家角色和键盘控制

- [ ] 实现 `Player` 类（位置、移动、碰撞检测）
- [ ] 实现键盘输入处理（WASD移动）
- [ ] 实现基于网格的平滑移动动画
- [ ] 实现与墙体的碰撞检测
- [ ] 测试：玩家能在地图中自由移动,无法穿墙

**交付物**: 可控制角色在地图中移动

---

#### **阶段四：炸弹系统（预计2-3天）**

**目标**: 实现炸弹放置、爆炸和破坏效果

- [ ] 实现 `Bomb` 类（倒计时、爆炸逻辑）
- [ ] 实现 `Explosion` 类（火焰渲染、持续时间）
- [ ] 实现炸弹放置（空格键）和数量限制
- [ ] 实现十字火焰扩散算法
- [ ] 实现软墙破坏逻辑
- [ ] 添加炸弹音效（放置、爆炸）
- [ ] 测试：炸弹能正确爆炸并摧毁软墙

**交付物**: 完整的炸弹-爆炸-破坏循环

---

#### **阶段五：道具系统（预计1-2天）**

**目标**: 实现道具掉落和效果

- [ ] 实现 `PowerUp` 类（三种道具）
- [ ] 实现道具生成器（软墙破坏后随机掉落）
- [ ] 实现道具拾取检测
- [ ] 实现道具效果应用（火焰长度、炸弹数量、速度）
- [ ] 添加拾取音效
- [ ] 测试：道具能正确掉落并生效

**交付物**: 可获取并生效的道具系统

---

#### **阶段六：敌人AI（预计2-3天）**

**目标**: 实现两种敌人的AI和碰撞判定

- [ ] 实现 `Enemy` 基类
- [ ] 实现 `BasicEnemyAI`（随机游荡）
- [ ] 实现 `ChaseEnemyAI`（追踪玩家）
- [ ] 实现敌人与玩家的碰撞判定
- [ ] 实现敌人被火焰消灭的逻辑
- [ ] 添加敌人死亡音效和动画
- [ ] 测试：敌人能正常移动、追踪、被消灭

**交付物**: 完整的敌人AI系统

---

#### **阶段七：游戏逻辑与UI（预计2天）**

**目标**: 实现胜利/失败条件和用户界面

- [ ] 实现游戏状态管理（进行中、胜利、失败）
- [ ] 实现胜利条件（消灭所有敌人+进入出口）
- [ ] 实现失败条件（被火焰/敌人击中）
- [ ] 实现 HUD 显示（剩余敌人、炸弹威力、数量）
- [ ] 实现结算画面（胜利/失败提示）
- [ ] 实现重新开始功能（R键）
- [ ] 测试：游戏流程完整可玩

**交付物**: 完整可玩的游戏原型

---

#### **阶段八：打包与发布（预计1-2天）**

**目标**: 生成跨平台可执行文件

- [ ] 编写 `requirements.txt`
- [ ] 编写 `build_config.py` 打包脚本
- [ ] 准备应用图标文件
- [ ] 在各平台执行打包测试
  - [ ] Windows: 生成 `.exe`
  - [ ] macOS: 生成 `.app`
  - [ ] Linux: 生成可执行文件
- [ ] 执行打包后测试清单
- [ ] 编写用户使用说明（README）

**交付物**: 三平台可执行程序包

---

### 8.2 关键里程碑

| 里程碑 | 完成标志 | 预计时间点 |
| :--- | :--- | :--- |
| **MVP原型** | 玩家可以移动、放炸弹、炸墙 | 第7天 |
| **完整Demo** | 所有功能实现,游戏可通关 | 第14天 |
| **发布版本** | 三平台打包完成,可分发 | 第16天 |

### 8.3 开发建议

1. **迭代式开发**: 每完成一个阶段立即测试,确保功能正常后再进入下一阶段
2. **版本控制**: 使用 Git 进行代码管理,每个阶段完成后打 tag
3. **占位资源优先**: 初期使用纯色方块作为图像资源,加速开发进度
4. **性能监控**: 开发过程中持续监控帧率,确保保持在60FPS
5. **文档同步**: 遇到设计变更时及时更新PRD文档

### 8.4 风险与应对

| 风险 | 影响 | 应对策略 |
| :--- | :--- | :--- |
| **Pygame学习曲线** | 延长开发周期 | 提前阅读官方文档,参考示例项目 |
| **跨平台兼容性问题** | 打包失败 | 优先在主要平台测试,使用虚拟机验证 |
| **性能问题** | 帧率下降 | 使用性能分析工具,优化渲染和碰撞检测 |
| **AI表现不佳** | 游戏体验差 | 调整AI参数,增加随机性和变化 |

---

**总结**: 此版本将所有资源集中于**一张地图、一种模式、一套键位**,旨在以最高效率验证《炸弹人》最核心的"探索-炸墙-杀敌-避险"循环。完整的技术实现方案、清晰的项目结构和详细的开发路线图,为快速启动开发提供了最直接的蓝图。
